service cloud.firestore {
  match /databases/{database}/documents {
    match /teams/arts/accounts/{account} {
      allow read, write: if request.auth.token.email_verified == true && request.auth.token.email.matches('.*@asiarisktransfer[.]com');
    }
    allow read, write: if false;
  }
}
